import { JoinDateCountPipe } from './join-date-count.pipe';

describe('JoinDateCountPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinDateCountPipe();
    expect(pipe).toBeTruthy();
  });
});
